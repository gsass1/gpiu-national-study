<p><%= t('devise.views.mailer.confirmation.welcome') %>> <%= @email %>!</p>

<p><%= t("devise.views.mailer.confirmation.confirm_text") %></p>

<p><%= link_to t("devise.views.mailer.confirmation.confirm"), confirmation_url(@resource, confirmation_token: @token) %></p>
